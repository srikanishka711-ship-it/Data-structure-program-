class Solution:
    def getKthFromLast(self, head, k):
        fast = head
        slow = head

        # Move fast pointer k steps
        for i in range(k):
            if fast is None:
                return -1
            fast = fast.next

        # Move both pointers
        while fast:
            fast = fast.next
            slow = slow.next

        return slow.data
